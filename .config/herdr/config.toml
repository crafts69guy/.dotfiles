onboarding = false
# herdr configuration — replaces tmux as the daily-driver multiplexer.
# See ~/.dotfiles/.claude/plans (or agent-rules/tools/herdr/) for the migration
# rationale. Full option reference: `herdr --default-config`.

[theme]
# name = "catppuccin"
name = "terminal"
auto_switch = false
# BEGIN hue-theme
[theme.custom]
# Generated by scripts/build.ts. Do not edit.
# Huế Hương — Hue theme for herdr.
# apply.sh also copies `accent` into [ui].accent; keep it first.
accent = "#6FD0A8"
panel_bg = "#182A24"
surface0 = "#244E42"
surface1 = "#587168"
surface_dim = "#244E42"
overlay0 = "#6C7E76"
overlay1 = "#8A968E"
text = "#D6DAD0"
subtext0 = "#98A59C"
mauve = "#A9A0C8"
green = "#6FD0A8"
yellow = "#DCBB80"
red = "#D38478"
blue = "#7FBAC8"
teal = "#7FBAC8"
peach = "#DCBB80"
# END hue-theme

[terminal]
# fish is the primary shell; empty default_shell falls back to $SHELL.
shell_mode = "login"
new_cwd = "follow"

[keys]
# tmux used ctrl+t as prefix (ctrl+b was unbound) — keep the same muscle memory.
prefix = "ctrl+t"

# reload_config, split_vertical/split_horizontal, focus_pane_*, and resize_mode
# all match herdr's defaults already and mirror tmux's vi-style h/j/k/l pane
# navigation, so they are left commented/default (see `herdr --default-config`).

# Workspace navigation mode: use vi-style vertical movement instead of arrows.
navigate_workspace_up = "k"
navigate_workspace_down = "j"

# Workspace switching follows left/right vi movement.
previous_workspace = "prefix+ctrl+k"
next_workspace = "prefix+ctrl+j"

# Agent navigation mirrors the vi-style direction keys and tab number jumps.
previous_agent = "prefix+alt+k"
next_agent = "prefix+alt+j"
focus_agent = "prefix+alt+1..9"

# Jump back to the previously focused pane without changing tab/workspace order.
last_pane = "prefix+backtick"

# Worktrees, mirroring `git worktree add/remove` — plain letters, no shift.
# a = add (default was prefix+shift+g), double-tap t = open an existing one,
# d = delete (opens confirmation; shift+d stays close_workspace).
new_worktree = "prefix+a"
open_worktree = "prefix+t"
remove_worktree = "prefix+d"

# Best-effort analog of tmux's `bind F run-shell "open '#{pane_current_path}'"`.
[[keys.command]]
key = "prefix+f"
type = "shell"
command = "open ."
description = "Open current pane's directory in Finder"

# Git tooling popups, ported from utility.conf's prefix+g/prefix+G. Those two
# keys collide with herdr's own defaults (goto = prefix+g, new_worktree =
# prefix+shift+g), so this uses a new prefix+alt+ group instead — no herdr
# default binding uses the alt modifier.
[[keys.command]]
key = "prefix+alt+g"
type = "pane"
command = "lazygit"
description = "Open lazygit"

[[keys.command]]
key = "prefix+alt+shift+g"
type = "pane"
command = "keifu"
description = "Open git graph (keifu)"

[ui]
mouse_capture = true
pane_borders = true
pane_gaps = true

agent_panel_sort = "spaces"
accent = "#6FD0A8"
show_agent_labels_on_pane_borders = false
[session]
resume_agents_on_restore = true

[ui.sound]
enabled = false

[ui.toast]
delivery = "terminal"

[experimental]
allow_nested = false
pane_history = true
switch_ascii_input_source_in_prefix = true
