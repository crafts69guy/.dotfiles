# Display lazygit
# bind -r g display-popup -d '#{pane_current_path}' -w90% -h90% -E lazygit

# Launch opencode
# bind -r O display-popup -d '#{pane_current_path}' -w90% -h90% -E opencode # new session mode
# bind -r o display-popup -d '#{pane_current_path}' -w90% -h90% -E opencode --continue # resume mode

